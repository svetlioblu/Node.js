<main>
    <!-- Start Add Photo Section -->
    <div class="editPage">
        <h2>Edit photo</h2>
        <!-- Start Add Photo Form -->
        <form method="POST">
            <p>
                <input type="text" name="name" placeholder="Name" value="{{oneCard.name}}">
            </p>

            <p>
                <input type="number" name="age" placeholder="Age" value="{{oneCard.age}}">
            </p>

            <p>
                <textarea type="text" name="description" placeholder="Description">{{oneCard.description}}</textarea>
            </p>

            <p>
                <input type="text" name="location" placeholder="Location" value="{{oneCard.location}}">
            </p>

            <p>
                <input type="text" name="image" placeholder="Link to image" value="{{oneCard.image}}">
            </p>
          
            <button class="edit-btn" type="submit">Edit Photo</button>
        </form>
    </div>
</main>